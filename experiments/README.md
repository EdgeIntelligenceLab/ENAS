# Experiments Workspace

This directory is intentionally kept empty in the repository. It is used as
a workspace for experimental scripts, exploratory analyses, and one-off
sanity checks that have not yet been promoted to the main `scripts/` or
`figures/scripts/` locations.

Anything reproducible and documented should live in `scripts/`.
